#include "Classifier.h"

namespace Multimedia {
    namespace Logical {
        void Classifier::OnMultimediaArraived(std::string const& localFilename) {
            //switch (type) {
            //    case tImage:
            //    case tMultimedia:
            //    case tText:
            //        decodeBase64(localFilename);
            //        saveFile(decodedFilename);
            //        saveMetaInfoToDB(); //the menu node info
            //        break;
            //    case tMMS:
            //        decodeBase64(localFilename);
            //        saveFile(decodedFilename);
            //        notifyMMS("a new mms received!", decodedFilename);
            //        break;
            //    case tWeather:
            //        decodeBase64(localFilename);
            //        saveFile(decodedFilename);
            //        saveMetaInfoToDB(); //the weather new icon info
            //        break;
            //    default:
            //        break;
            //}
        }
    }
}

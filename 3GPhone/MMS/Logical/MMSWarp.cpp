#include "MMSWarp.h"

namespace MMS {
    namespace Logical {
        void MMSWarp::OnMMSArraived(std::string const& localFilename) {
            //partition the file
            //save meta info to db
            if (hasSmil) {
                //parse the smil
            } else {
                //default layout
            }
            //render the content
            //show it
        }
    }
}
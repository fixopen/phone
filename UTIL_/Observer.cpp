#include "Observer.h"

namespace Util {
	Observer::~Observer()/* = 0 */{}
    //void Observable::RegisterObserver(Observer* const ob) {
    //    observers_.push_back(ob);
    //}

    //void Observable::Notify(std::string const& data) {
    //    for (size_t i = 0; i < observers_.size(); ++i) {
    //        observers_[i]->Update(data);
    //    }
    //}
}

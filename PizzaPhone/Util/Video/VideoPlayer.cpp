#include "VideoPlayer.h"

namespace Util {
    namespace Video {
        void VideoPlayer::Start() {
        }

        void VideoPlayer::Previous() {
        }

        void VideoPlayer::Next() {
        }

        void VideoPlayer::Stop() {
        }

        void VideoPlayer::SetVolume(size_t const volume) {
        }

        void VideoPlayer::SetLightness(size_t const lightness) {
        }

        void VideoPlayer::SetContract(size_t const contract) {
        }

        TimeSpan const VideoPlayer::GetCurrentMediaDuration() const {
            TimeSpan result;
            return result;
        }

        size_t const VideoPlayer::GetCurrentMediaPlayPrecent() const {
            size_t result = 0;
            return result;
        }
    }
}

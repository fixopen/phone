#include "Camera.h"

namespace Util {
    namespace Video {
        Camera::Camera(CameraParameters const& params) {
        }

        Camera::~Camera() {
        }

        void Camera::Start() {
        }

        void Camera::Stop() {
        }
    }
}

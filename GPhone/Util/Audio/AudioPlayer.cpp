#include "AudioPlayer.h"

namespace Util {
    namespace Audio {
        void AudioPlayer::Start() {
        }

        void AudioPlayer::Previous() {
        }

        void AudioPlayer::Next() {
        }

        void AudioPlayer::Stop() {
        }

        void AudioPlayer::SetVolume(size_t const volume) {
        }

        void AudioPlayer::SetLightness(size_t const lightness) {
        }

        void AudioPlayer::SetContract(size_t const contract) {
        }

        TimeSpan const AudioPlayer::GetCurrentMediaDuration() const {
            TimeSpan result;
            return result;
        }

        size_t const AudioPlayer::GetCurrentMediaPlayPrecent() const {
            size_t result = 0;
            return result;
        }
    }
}

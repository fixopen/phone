#include "ImagePlayer.h"

namespace Util {
    namespace Video {
        void ImagePlayer::Start() {
        }

        void ImagePlayer::Previous() {
        }

        void ImagePlayer::Next() {
        }

        void ImagePlayer::Stop() {
        }

        void ImagePlayer::SetVolume(size_t const volume) {
        }

        void ImagePlayer::SetLightness(size_t const lightness) {
        }

        void ImagePlayer::SetContract(size_t const contract) {
        }

        TimeSpan const ImagePlayer::GetCurrentMediaDuration() const {
            TimeSpan result;
            return result;
        }

        size_t const ImagePlayer::GetCurrentMediaPlayPrecent() const {
            size_t result = 0;
            return result;
        }
    }
}
